import React, {Component} from 'react';
import PropTypes from 'prop-types';

const Workspace = () => {
		return (
			<div>
				<canvas>Your browser doesn't not support canvas API. Upgrade your browser to be able to edit photos.</canvas>
			</div>
		);
}

Workspace.propTypes = {};

export default Workspace;